[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
startCommand = "osrm-routed --algorithm mld --max-table-size 10000 /data/turkey.osrm"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
